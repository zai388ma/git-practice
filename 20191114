###项目学习笔记
- 关于scrollTop的问题？
- 关于this.$nextTick(callback)：类似于浏览器的异步加载，把涉及到用到更新后的数据的操作都放在一个回调函数中，等更新完成之后再来执行这个回调函数。
浏览器的异步加载是模块的加载不影响它后面语句的执行，所有依赖这个模块的语句，都定义在一个回调函数中，等到加载完成之后，这个回调函数才会运行。
- gulp.js是什么东西？
- [在项目中使用vue-quill-editor富文本编辑器](https://juejin.im/post/5d8c66a2e51d45783544b9ae)
- [git详细解答](https://segmentfault.com/q/1010000009549291)
- [如何处理前端异常](https://blog.fundebug.com/2018/12/07/how-to-handle-frontend-error/)
- render()函数某种程度上也算是一个钩子函数。
- 数组还有个filter方法可以生成过滤之后的新的数组。如list.filter(item=>item<10)
- [大白话讲解promise](https://www.cnblogs.com/lvdabao/p/es6-promise-1.html)
- 什么是端口映射？
- CSS浮动float父div没有高度的问题
- bad Request和404的区别？
- 把params换成data参数就会在请求体里面而不是url中。
- token时间间隔不能太长。
- 判断buttons数组中是否有auth这个元素，如果indexOf是-1，就不含这个元素：buttons.indexOf(auth)===-1
- 硬性重新加载不使用浏览器缓存数据，直接到服务器上读取当前网址数据，并将从服务器上获取的数据覆盖旧的缓存，其他的网页缓存不受影响。清除缓存重新加载 是 先清除整个缓存，既然没有缓存就必须到服务器中获取数据，并进行新的缓存。（不确定是清除整个缓存，还是当前网址的缓存，整个缓存可能性很大，如果只是清除当前网址的缓存就跟硬性重新加载效果一样。有空试验下）
- 请求的函数形参要和后端的形参一样，但是子组件触发父组件的事件并传递参数时形参不用相同，优先前面的一一对应。
- java里面的空指针？
- watch中的变量用引号括起来？
- ...mapGetters（[permission]）是什么意思？
- row是一行横着的数据，column是竖着的一列。
- loading（）和done()是avue组件自带的。
- 请求函数应该是returnl一个promise对象，所以才可以用then(),也可以用all和race函数。
- if (["edit", "view"].includes(type)也可以用来判断数组中是否包含type这个元素
- width和height的百分比是相对于外层元素的吗？
- 子组件触发父组件的事件时，父组件中接受的参数数量不一定和传递的数量要一致，但是父组件参数的前几个依次和子组件传参的前几个是一样的
- vue的sync修饰符。
- height的作用一级一级撑起来的，高度没撑满滚动条是不会出现的？
- 在带有scoped标签的样式里面，改不成el组件的样式，要放在没有scoped标签的地方改。
- 网页加载速度太慢如何解决？是哪里的问题？
- 请求返回的状态码是200才能执行then这一步。
- 状态码拦截器可以设置不同状态码对应显示的错误信息。
- 通过对谷歌浏览器的设置，可以移除localhost这个网站的密码，就不会新增的时候密码已经填好。
